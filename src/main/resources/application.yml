springdoc:
  api-docs:
    enabled: true

spring:
  jpa:
    open-in-view: false
  cache:
    cache-names: temaconteudos,niveisensino,disciplinas
    caffeine:
      spec:
        maximumSizes: 10000
        expireAfterAccess: 600

server:
  port: 8081

api:
  security:
    token:
      issuer: "obama-api"
